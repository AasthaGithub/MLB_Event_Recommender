# -*- coding: utf-8 -*-
"""EventType.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1gk9L7NKnjEBvfu9ngpPR8YI4vSBT63vq
"""

"""
Dependencies: sklearn,pandas,pickle
"""

from sklearn.feature_extraction.text import CountVectorizer
from sklearn.feature_extraction.text import TfidfTransformer
from sklearn.naive_bayes import MultinomialNB
from sklearn.linear_model import SGDClassifier
from sklearn.model_selection import train_test_split
import pandas as pd

df = pd.read_csv('EventType.csv')

# Train -Test Split of data
train_x, test_x, train_y, test_y = train_test_split(df['Event title'], df['Event type'], test_size=0.2,random_state=11)

from sklearn.pipeline import Pipeline
# text_clf = Pipeline([('vect', CountVectorizer()),('tfidf', TfidfTransformer()),('clf', MultinomialNB())])
text_clf = Pipeline([('vect', CountVectorizer()),('tfidf', TfidfTransformer()),('clf-svm', SGDClassifier(loss='hinge', penalty='l2',alpha=1e-3, random_state=11)),])
text_clf = text_clf.fit(train_x, train_y)

import numpy as np
predicted = text_clf.predict(test_x)
np.mean(predicted == test_y)

"""
Code to Test: 
predicted_test = text_clf.predict(['Python is necessary for Artificial Intelligence AI to propel workshop', 'certify your python skills'])
print(predicted_test)
"""

import pickle

# Save to file in the current working directory
pkl_filename = "pickle_event_type_model.pkl"
with open(pkl_filename, 'wb') as file:
    pickle.dump(text_clf, file)

# Load from file
with open(pkl_filename, 'rb') as file:
    pickle_model = pickle.load(file)
    
# Calculate the accuracy score and predict target values
score = pickle_model.score(test_x, test_y)
print("Test score: {0:.2f} %".format(100 * score))
Ypredict = pickle_model.predict(test_x)


"""
This is a file to predict EventType of any event given its Title as Input
MLB & SGD classifier Approaches were tried on the training Set EventType.csv
& found out SGD works better , getting more accurate results; hence SGD text classifier was built & finalized.
"""

